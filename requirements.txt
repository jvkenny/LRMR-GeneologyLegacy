# LRGDM Python dependencies (Postgres-backed tooling)
# Install:  python3 -m pip install -r requirements.txt
#   (Homebrew python is PEP-668 externally-managed; add --break-system-packages
#    or use a venv.)
psycopg[binary]>=3.2

# Blob-storage backend (apply_scan.py LRGDM_MEDIA_BACKEND=blob).
# The SDK path authenticates via your existing `az login` (DefaultAzureCredential)
# and avoids the CLI's account-key-query warning. (If absent, apply_scan still
# falls back to shelling out to `az storage blob upload`.)
azure-storage-blob>=12
azure-identity>=1.15
