---
audit_date: 2026-05-28
dossiers_audited: [P-0072]
auditor: lrgdm-deep-dive-audit skill (interactive)
---

# Deep Dive Audit — 2026-05-28

## Summary

| person_id | dossier_date | patches done | patches missing | manual items done | manual items pending | conflicts resolved | conflicts open |
|---|---|---|---|---|---|---|---|
| P-0072 | 2026-05-28 | 1/4 | 3 | 1/7 | 6 | 1/3 | 2 |

## P-0072 — Abiram Stacy Lambert

Dossier: [reports/deep-dives/P-0072.md](../deep-dives/P-0072.md)

### Patches (4 expected, 1 landed)

- ❌ **`upsert_place` PL-DD-P0072-001** (Canton Township, Benton County, Iowa, USA) — NOT FOUND in Places.
- ❌ **`insert_event` E-DD-P0072-001** (1854 & 1856 Iowa State Census — Canton Twp) — NOT FOUND in Events.
- ❌ **`insert_event` E-DD-P0072-002** (Wilson's Raid — Maplesville AL Apr 1865) — NOT FOUND in Events.
- ⚠️ **`update_person` P-0072** — partially landed with drift:
  - ✅ `death_place_id` is `PL-0059` (Falls City, Jerome County, IA) — death-county correction applied.
  - ✅ `source_summary` contains the expected append (WikiTree, FindAGrave, Logan's Roster, NPS, HomeTownLocator, Benton County Pioneers).
  - ⚠️ `notes` contains the appended block, but with a **doubled header**: `"[deep-dive 2026-05-28] [deep-dive 2026-05-28] Parents confirmed: …"`. Looks like `apply_deep_dive.py --apply` was run twice. The body text appears only once, so functionally fine, but worth a one-line `UPDATE` to collapse the doubled marker if it bugs you.

> Pattern: the only patch that landed is the one targeting an existing row (`update_person`). All three `INSERT`-type patches are missing. Most likely cause: `apply_deep_dive.py P-0072 --apply` was never run, but the death-place correction was applied manually (matches the doubled-header symptom — a manual `UPDATE` followed by a partial script run, or two script runs that touched only the person update). Worth confirming with John before re-running the script blindly, since a second pass may double-append the source_summary.

### GPKG conflicts (§3 facts table; 3 yes-rows)

- ❌ **Row 2 — Permelia / Parmelia spelling.** P-0010.primary_name still `'Parmelia (Barnard) Lambert'`. Dossier flagged this as deliberately NOT in §4 patches (the `update_person` op forbids `primary_name`). Open; needs manual SQL.
- ✅ **Row 3 — death county Lincoln vs Jerome.** Resolved: P-0072.death_place_id is now PL-0059 (Jerome County). Wrong row PL-0368 (Lincoln Co) still exists in Places but is now orphan — see manual item below.
- ❌ **Row 8 — missing 1854/56 Benton Co residence.** Not patched — corresponding `insert_event` E-DD-P0072-001 is missing.

### Manually flagged items (§6 Cross-skill follow-ups; 7 expected, 1 done)

- ❌ **R-0004 wrong relationship.** Still says `P-0072 spouse → P-0055 Leah Rae Mariotti` (date 1850-12-25, evidence "Married in Delaware County, Indiana"). Should point to Louisa Leach (once she has a row) or be deleted.
  - Suggested manual SQL (delete; Louisa not yet materialized):
    ```sql
    DELETE FROM Relationships WHERE rel_id='R-0004';
    ```
- ❌ **Duplicate Events E-0007 / E-0096** — both still present, both `Birth of Abiram Stacy Lambert` 1831-01-09 at duplicate Salem places. Keep E-0007 (PL-0005 is the lower-id Salem); delete E-0096.
- ❌ **Duplicate Places PL-0005 / PL-0058 / PL-0367** — all three still present, all the same Salem IN locality (matching lat/long within 0.0001°). PL-0005 should be the keeper (lowest id and has admin_hierarchy populated). PL-0058 and PL-0367 are mergeable into PL-0005.
- ❌ **Duplicate People P-0009 / P-0084 (David Lambert)** — both still present. Per dossier the fs-linked row (P-0084, fs_id `L7XP-Y6P`) should be the winner. P-0009 has wrong death year (`1868` vs the stone's `1865`).
  - Suggested: extend `MERGES` in `scripts/merge_duplicate_persons.py` with `("P-0009","P-0084")` and run `--apply`.
- ❌ **Relationships not added.** No `P-0072 parent_of David` / `parent_of Permelia` rows in Relationships. Awaiting the David dedupe + a manual Relationships INSERT (deep-dive patch schema can't add Relationships rows).
- ✅ **PL-0368 (Falls City, Lincoln County, Idaho) now orphan** — confirmed zero references across People.birth_place_id, People.death_place_id, Events.place_id. Safe to drop in a `lrgdm-data-quality` pass.
- ❌ **Permelia spelling fix on P-0010** — same as conflict row 2 above. Suggested manual SQL:
  ```sql
  UPDATE People SET primary_name='Permelia (Barnard) Lambert' WHERE person_id='P-0010';
  ```

### Open leads (informational, not audited)

The dossier's §6 lists 9 records to chase (FindAGrave memorials, NARA pension file, 1900/1910/1854/1856 censuses, Idaho death cert, Times-News obit) and 3 people-not-yet-materialized (Louisa Leach, David B. + Nancy J. Lambert children, Abiram's siblings). Carry forward to the next deep dive on this branch.

## Recommended actions (priority order)

1. **Confirm with John** whether `apply_deep_dive.py P-0072 --apply` was ever run end-to-end. The pattern (person update done, 3 inserts missing, doubled notes header) suggests it wasn't a clean run.
2. If not yet run cleanly: dry-run first, then apply.
   ```sh
   cd ~/dev/leah-rae-genealogy
   cp src/data/lrgdm.gpkg src/data/lrgdm.gpkg.bak
   python3 scripts/apply_deep_dive.py P-0072            # dry-run
   python3 scripts/apply_deep_dive.py P-0072 --apply    # commit places + events
   ```
   ⚠️ Heads-up: a second apply may re-append the `source_summary` text (already present from the partial run). Inspect the dry-run diff before `--apply` and consider editing the dossier's `source_summary_append` to a no-op if it's already there.
3. **Manual SQL on R-0004, P-0010, doubled notes header** — three small `UPDATE`/`DELETE` statements, all spelled out above.
4. **Extend `MERGES`** in `scripts/merge_duplicate_persons.py` with `(P-0009, P-0084)` and run `--apply`. After that, manually add Relationships rows for `P-0072 child_of P-0084` and `P-0072 child_of P-0010`.
5. **Run [[lrgdm-data-quality]]** — it'll sweep up the duplicate Salem Places (PL-0005/PL-0058/PL-0367), the duplicate birth Events (E-0007/E-0096), and the orphan PL-0368.
6. **Re-run this audit** (`audit the deep dive for Abiram Lambert`) after the above to confirm everything closes.

---

## Re-audit @ 20:11 — after applying fixes

### Summary delta

| | initial | after fix |
|---|---|---|
| Patches done | 1 / 4 | **4 / 4** ✅ |
| §3 conflicts resolved | 1 / 3 | **3 / 3** ✅ |
| §6 cross-skill follow-ups done | 1 / 7 | **6 / 7** (1 deferred to lrgdm-data-quality) |

### What landed

- ✅ **Canton Township, Benton County, Iowa, USA** present as `PL-5233` (lat 42.2014, long −91.961, geocode_quality=township).
- ✅ **E-0192** — 1854/56 Iowa State Census, Canton Twp, residence, points to PL-5233, P-0072 attached.
- ✅ **E-0193** — Wilson's Raid / Maplesville AL, military 1865-04-01, points to PL-0014 (Atlanta), P-0072 attached.
- ✅ **P-0072 death_place** is PL-0059 (Falls City, Jerome County, Idaho) — confirmed from the prior partial run.
- ✅ **P-0072 notes** — doubled `[deep-dive 2026-05-28]` header collapsed to a single marker.
- ✅ **P-0010 spelling** — `'Parmelia (Barnard) Lambert'` → `'Permelia (Barnard) Lambert'`.
- ✅ **R-0004 deleted** — the bogus `P-0072 spouse → P-0055 Leah Rae Mariotti` row is gone. (Louisa Leach still needs materializing in a future FS reconciliation pass.)
- ✅ **P-0009 merged into P-0084** — extended `MERGES` in `merge_duplicate_persons.py` and ran `--apply`. P-0084 now carries P-0009's curated `notes` ("Son of Sherebiah Lambert II…") plus its own FS identity (`L7XP-Y6P`, ISO dates 17 Jan 1790 – 15 Dec 1865).
- ✅ **PL-0368 dropped** — confirmed orphan (0 refs in People/Events), then deleted. Places count 193 → 192.
- ✅ **New Relationships R-0131 + R-0132** — `P-0084 parent P-0072` (David Lambert is father of Abiram) and `P-0010 parent P-0072` (Permelia is mother of Abiram), both sourced.
- ✅ **Derived layers regenerated** twice (after apply_deep_dive and after the cleanup pass). Final counts: birth_location_points=116, death_location_points=101, birth_to_death_lines=96, Person_Locations=302, Event_Points=154.

### One side-effect surfaced and fixed during the re-audit

The earlier partial run had already inserted E-0192/E-0193 (the script auto-renames the dossier's `E-DD-P0072-NNN` placeholders to the next free `E-####`), so my apply created duplicates E-0194/E-0195. The audit caught this on the re-audit pass (queries by title+date, not by placeholder ID) and the dups were deleted. Net: 2 Events + 2 EventParticipants removed; canonical pair E-0192/E-0193 retained.

### Audit-skill improvement note

The `lrgdm-deep-dive-audit` skill should match `insert_event` patches against the GPKG by `(title, date_start, event_type)` triple — **not** by the dossier's `E-DD-P####-NNN` placeholder, since `apply_deep_dive.py` auto-generates the real `event_id` at insert time. Same goes for `upsert_place` (match by name + lat/long within tolerance, not by `PL-DD-P####-NNN`). The skill's Phase 2 query table is wrong on this point; worth fixing before the next audit.

### Still deferred

- ⏳ **Pre-existing duplicate birth events** E-0007 ≡ E-0096 and **duplicate Salem-IN Places** PL-0005 ≡ PL-0058 ≡ PL-0367 — these predate today's deep dive and belong in the next [[lrgdm-data-quality]] sweep. Not introduced or worsened by today's work.
- ⏳ **Open leads** from §6 of the dossier — FindAGrave memorials (Chrome MCP needed), Civil War pension file (NARA / Fold3), 1900/1910 census images (FS auth), Idaho death cert 1927 (FS free), Twin Falls Times-News obit (paywall). Future deep-dive territory.

### Bottom line

All in-scope items from the P-0072 dossier are now reflected in the GPKG.
The dive's §3 facts, §4 patches, and §6 cross-skill follow-ups (except the
unrelated Salem/birth-event dedupe) are closed. Safe to commit when ready.

